# Example .env file for S3-compatible storage
# Copy this to .env and fill in your credentials:
#   cp .env.example .env
#
# These variables are loaded by pipelines that specify:
#   env_file: ./.env

# S3 Endpoint URL (required for non-AWS S3-compatible storage)
# Examples:
#   MinIO:           http://localhost:9000
#   Nutanix Objects: https://objects.nutanix.local:443
#   LocalStack:      http://localhost:4566
#   AWS S3:          (leave blank or omit - uses AWS defaults)
AWS_ENDPOINT_URL=https://objects.nutanix.local:443

# AWS/S3 Credentials
AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-key-here

# Region (defaults to us-east-1 if not set)
AWS_REGION=us-east-1

# SSL Certificate Verification (optional)
# Set to "true" if your S3 endpoint has valid SSL certificates
# Set to "false" for self-signed certs (MinIO, local development)
AWS_S3_VERIFY_SSL=false
