"""Backward compatibility shim for core.config."""
from core.infrastructure.config import *
from core.infrastructure.config import __all__
from core.infrastructure.config import loaders, models, validation, migration, placeholders
