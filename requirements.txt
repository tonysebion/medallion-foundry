# Core dependencies - required for all installations
polars>=1.0.0
pandera>=0.20.0
pydantic-settings>=2.1.0
structlog>=24.1.0
boto3>=1.26.0
pandas>=1.5.0
pyyaml>=6.0
requests>=2.28.0
httpx>=0.27.0
requests-toolbelt>=1.0.0
tenacity>=8.0.0
ibis-framework[duckdb]>=9.0.0  # Portable dataframe library with DuckDB backend
s3fs>=2023.1.0  # S3 filesystem implementation for fsspec
pyodbc>=4.0.0  # ODBC support for database extractors (DB2)

# Install with: pip install -r requirements.txt
#
# Optional dependencies (install separately if needed):
# - Azure: pip install azure-identity azure-storage-blob adlfs
# - TUI: pip install -r requirements-tui.txt
# - Dev: pip install -r requirements-dev.txt
